# Copy this file to .env.local and fill in the values

# Password to access the dashboard (shown on login screen)
DASHBOARD_PASSWORD=your-secret-password

# Secret used to sign the auth cookie (use a long random string)
AUTH_SECRET=replace-with-a-long-random-string-32-chars-minimum
